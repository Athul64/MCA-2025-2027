CREATE TABLE Emp(
    EmpNo NUMBER,
    EmpName VARCHAR2(30),
    Job VARCHAR2(30),
    Basic NUMBER,
    DA NUMBER,
    HRA NUMBER,
    PF NUMBER,
    GrossPay NUMBER,
    NetPay NUMBER
);

INSERT INTO Emp(EmpNo,EmpName,Job,Basic,DA,HRA,PF,GrossPay,NetPay)
VALUES(1,'RAM','MANAGER',20000,6000,8000,2000,34000,32000);

INSERT INTO Emp(EmpNo,EmpName,Job,Basic,DA,HRA,PF,GrossPay,NetPay)
VALUES(2,'ASWIN','CLERK',8000,2400,3200,1000,13600,12600);

SELECT * FROM Emp
WHERE GrossPay BETWEEN 10000 AND 20000;

SELECT * FROM Emp
WHERE NetPay=(SELECT MAX(NetPay) FROM Emp);

UPDATE Emp
SET NetPay = NetPay + 1200
WHERE NetPay<10000;